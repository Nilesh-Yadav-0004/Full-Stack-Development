// this pre class video concept

// clouser topics

// clouser is related to function...

// this is the parent function

const justFunction = () => {
    let n = 'Nilesh';

    return () => {

    };
};

console.log(justFunction());

// this is the one way to invoke the functions

// :- it's closes the variable that are dependence it's like frezzes them it's doesn't allow them to 
// destroyed only there is some dependence, was -> A child function need a 
// variable from a parent function even those parent function had been called or after the function called the 
// variable destroyed but the parent func share the variable to the child function... 

// closure 

